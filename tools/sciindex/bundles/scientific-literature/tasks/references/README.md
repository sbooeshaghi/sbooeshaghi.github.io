# References task

This task inventories references and connects each used reference to accepted
atomic claim IDs. Used references retain exact citation-bearing text. The
validator requires those spans to exist in the paper and to overlap the exact
evidence of every linked claim.

Reference metadata, claims, and evidence remain task-local observations until a
dataset adapter resolves the reference to a canonical publication object.
